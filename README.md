# word-count

A tiny CLI utility that counts word occurrences in a text file.

## Usage

```bash
python word_count.py path/to/file.txt
```

Prints each distinct word and how many times it appears, ignoring case and
surrounding punctuation.

## Tests

```bash
pip install pytest
pytest
```
